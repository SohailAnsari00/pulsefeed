services:
  - type: web
    name: my-flask-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: waitress-serve --port=$PORT app:app
    envVars:
      - key: FLASK_ENV
        value: production
